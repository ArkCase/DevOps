apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: grafana-np
  labels:
    managed-by: setup-cluster.sh
spec:
  podSelector:
    matchLabels:
      app: grafana
  ingress:
    - from:
        - ipBlock:
            cidr: 0.0.0.0/0
      ports:
        - port: 3000
